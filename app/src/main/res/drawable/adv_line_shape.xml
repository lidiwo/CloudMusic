<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line">
    <stroke
        android:width="1px"
        android:color="@color/gray_color4"
        android:dashGap="10px"
        android:dashWidth="0px" />
    <size
        android:width="20dp"
        android:height="1dp" />

    <!--
    显示一条虚线，width为线条的高度，dashWidth为破折线的宽度，
    dashGap为破折线之间的空隙的宽度，当dashGap=0时，就是实线.
    如果在<stroke>标签中设置了android:width，则在<View>标签中
    android:layout_height的值必须大于android:width的值，否则虚线不会显示。
    如果不设置，默认android:width为0。-->
</shape>